version: '3'
services:
  api:
    build:
      context: .
      dockerfile: dockerfile.api
    ports:
      - "8080:8080"
    networks:
      - backend

  php:
    build:
      context: .
      dockerfile: dockerfile.php
    ports:
      - "9000:9000"
    depends_on:
      - mysql
    networks:
      - backend

networks:
  backend: